---
import Base from '../../layouts/Base.astro';
import Hero from '../../components/Hero.astro';
import { LAB } from '../../data/lab';
---
<Base title="Lab" jpHeadings={false}>
  <Hero title="Lab" lead="ブラウザで動かせる実験を置いています。" />
  <ol role="list" class="divide-line">
    {LAB.map((l) => (
      <li class="reveal">
        <a href={`/lab/${l.slug}/`} class="group grid sm:grid-cols-[6rem_1fr] gap-x-4 gap-y-1 py-4">
          <span class="text-xs leading-4 text-muted sm:pt-1 tabular-nums">{l.date.replaceAll('-', '.')}</span>
          <div>
            <h2 class="display text-lg group-hover:text-accent-ink transition-colors">{l.title}</h2>
            <p class="text-sm text-ink-2 mt-2">{l.description}</p>
          </div>
        </a>
      </li>
    ))}
  </ol>
</Base>
